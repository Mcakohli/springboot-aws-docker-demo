version: '3.8'

services:
  app:
    build: .
    container_name: springboot-aws-demo
    ports:
      - "8080:8080"
    environment:
      - SPRING_DATASOURCE_URL=jdbc:postgresql://springboot-db.c3886ayam0s8.ap-south-1.rds.amazonaws.com:5432/testdb
      - SPRING_DATASOURCE_USERNAME=postgres
      - SPRING_DATASOURCE_PASSWORD=${SPRING_DATASOURCE_PASSWORD}
      - AWS_REGION=ap-south-1
      - AWS_S3_BUCKET=springboot-aws-demo-kohli